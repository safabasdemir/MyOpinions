@model List<Post>

<div class="slider">
    <div id="coin-slider">
        <a href="#">
            @for (int i = Model.Count - 1; i >= 0; i--)
            {
                <img src="~/images/@Model[i].PictureName" width="940" height="310" alt="" class="fl" />
            }

        </a>
    </div>
    <div class="clr"></div>
</div>